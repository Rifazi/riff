{
  "name": "meetily-harness-server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Local-only agent server (requirements / plan / coding / QA) behind Meetily's Dev Sessions UI. Started automatically by the Meetily desktop app; not deployed.",
  "workspaces": [
    "backend"
  ],
  "scripts": {
    "postinstall": "node scripts/ensure-env.js",
    "dev": "npm run dev --workspace=backend",
    "start": "npm install && npm run dev",
    "serve": "npm run serve --workspace=backend",
    "build": "npm run build --workspace=backend",
    "typecheck": "tsc -p backend/tsconfig.json --noEmit"
  }
}
